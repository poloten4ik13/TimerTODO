import React, {useState} from 'react';

const AddButtons = () => {
    const [buttons, setButtons] = useState([]);
    const addButton = () => {


    }
    return (
        <div>

        </div>
    );
};

export default AddButtons;
